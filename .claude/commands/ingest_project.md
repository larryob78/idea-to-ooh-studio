# Command: ingest_project

Ingest script/budget CSVs (+ optional schedule):

```bash
sba project ingest --project <slug> --script <path> --budget <path> [--schedule <path>]
```

Check `projects/<slug>/outputs/ingest_report.json` for warnings and limitations.
