# Command: compare_snapshots

Compare two snapshots for a project:

```bash
sba project compare --project <slug> --a <snapshot_id_a> --b <snapshot_id_b>
```

Check `projects/<slug>/outputs/snapshot_diff.md`.
